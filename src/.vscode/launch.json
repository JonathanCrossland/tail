{
    "version": "0.2.0",
    "configurations": [
      {
        "name": ".NET Core Launch (web)",
        "type": "coreclr",
        "request": "launch",
        "preLaunchTask": "build",
        "program": "${workspaceFolder}/bin/Debug/net9.0/tail.exe",
        "args": ["--test"],  // <-- Add your arguments here
        "cwd": "${workspaceFolder}",
        "stopAtEntry": false,
      }
    ]
  }
  